%h2
  = link_to l(:label_hr_adaptive_issue_plural), hr_adaptive_issues_path
  &#187;
  =h @hr_adaptive_issue

- form = labelled_form_for @hr_adaptive_issue do |f|
  = render :partial => 'form', :locals => {:f => f}
  = submit_tag l(:button_save)
= form if Rails::VERSION::MAJOR < 3
