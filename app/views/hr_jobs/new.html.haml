%h2
  = link_to l(:label_hr_job_plural), hr_jobs_path
  &#187;
  =l(:label_hr_job_new)

- form = labelled_form_for @hr_job do |f|
  = render :partial => 'form', :locals => {:f => f}
  = submit_tag l(:button_create)
= form if Rails::VERSION::MAJOR >= 3
